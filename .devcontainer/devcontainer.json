{
  "name": "gimnasio",
  "dockerComposeFile": "docker-compose.yml",
  "service": "workspace",
  "workspaceFolder": "/workspace",
  "forwardPorts": [3000, 3001, 4321],
  "postCreateCommand": "npm install --prefer-offline --no-audit --no-fund && npm run build --workspace=@gimnasio/shared && npm run prisma:generate --workspace=api && (test -f apps/api/.env || cp apps/api/.env.example apps/api/.env)",
  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "prisma.prisma",
        "ms-vscode.vscode-typescript-next",
        "bradlc.vscode-tailwindcss"
      ],
      "settings": {
        "typescript.tsdk": "node_modules/typescript/lib",
        "files.eol": "\n"
      }
    }
  }
}
